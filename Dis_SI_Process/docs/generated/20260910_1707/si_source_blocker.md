# Figure 5 V7R2 SI source blocker (superseded)

An interrupted pre-fix documentation attempt recorded a pandas attribute-access
error while formatting the paragraph (`float()` received the `mean` method).
The writer was corrected to access the named `"mean"` column and rerun for this
release. The current documentation QA is `pass`; this historical record is
kept so the failed intermediate check is not silently discarded.
