defaults: [plain_defaults.yaml]
model:
  name: latent_fm
  stage: 2
  latent_channels: 16
  # Required: set to the immutable output of latent_fm_stage1.yaml.
  stage1_checkpoint: null
output: {experiment_name: latent_fm_stage2}
