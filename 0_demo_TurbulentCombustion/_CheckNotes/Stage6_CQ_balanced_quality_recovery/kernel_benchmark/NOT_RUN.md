# Kernel benchmark — not run

The runbook requires MHA-mask versus SDPA and optional fused AdamW benchmarking
only after a scientific architecture candidate is selected. Neither the 192-D
primary nor the 224-D fallback cleared the pre-training efficiency gate, so no
scientific candidate was selected and kernel changes were not introduced.
